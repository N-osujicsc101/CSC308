#include <stdio.h> //standard input output header

// our main function is an int data type so we must return with an integer
// declare a variaable before you use it
// once you use double quote its not longer a character
// put function
// this [] with char denotes that its a string
// define a function before you use it in the main

void greetings;

    int main()
{

    int age;
    char gender = 'M';
    char name[20];

    // printf  ( "%c" , gender);

    // print9f("Enter your age: ");
    // scanf("%d", name);

    // printf("Hello %d year old!!\n", age);
    greetings();

    return 0;
}